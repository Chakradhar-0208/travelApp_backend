name: CI

on:
  push:
    branches:
    - main
    - kiran
    - harshini
    - chakree
  pull_request:
    branches:
    - main
    - kiran
    - harshini
    - chakree
  
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      # - name: Run tests
      #   run: npm test
<<<<<<< HEAD
      # need to write tests
=======
      # need to write tests
>>>>>>> kiran
